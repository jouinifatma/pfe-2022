{% extends 'base.html.twig' %}

{% block title %}Mes offres{% endblock %}

{% block body %}
    <div class="container">
        <h1>Mes offres

            <a href="{{ path('app_offer_new') }}" class="btn btn-primary float-right">Create new</a>
        </h1>

        <table class="table table-striped table-bordered">
            <thead>
            <tr>
                <th>Titre</th>
                <th>Date d'ajout</th>
                <th>Début</th>
                <th>Fin</th>
                <th>Etat</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            {% for offer in offers %}
                <tr>
                    <td><a href="{{ path('app_offer_show', {'id': offer.id}) }}">{{ offer.title|capitalize }}</a></td>
                    <td>{{ offer.createdAt ? offer.createdAt|date('d/m/Y') : '' }}</td>
                    <td>{{ offer.startAt ? offer.startAt|date('d/m/Y') : '' }}</td>
                    <td>{{ offer.endAt ? offer.endAt|date('d/m/Y') : '' }}</td>
                    <td>{{ offer.status }}</td>
                    <td>

                        <a href="{{ path('app_offer_edit', {'id': offer.id}) }}"><i class="fas fa-edit"></i> </a>
                        <a href="{{ path('app_offer_edit', {'id': offer.id}) }}"><i class="fas fa-trash-alt"></i> </a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="8">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
